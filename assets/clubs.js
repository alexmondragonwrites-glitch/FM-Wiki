const clubs=window.FM_CLUBS||[];const esc=v=>String(v??'').replace(/[&<>\"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;',"'":'&#39;'}[c]));const search=document.querySelector('#clubSearch'),scope=document.querySelector('#scopeFilter'),status=document.querySelector('#clubStatusFilter'),target=document.querySelector('#clubDirectory');const label={complete:'Vollständig',partial:'Teilprofil',template:'Vorlage'};function render(){const q=search.value.toLowerCase().trim(),s=scope.value,st=status.value;const list=clubs.filter(c=>(!q||[c.name,c.country,c.league,c.summary,...c.tags].join(' ').toLowerCase().includes(q))&&(s==='all'||c.scope.includes(s))&&(st==='all'||c.status===st));target.innerHTML=list.map(c=>`<a class="club-card ${c.status}" href="klub.html?id=${c.id}"><div class="club-card-head"><span class="club-mark">${esc(c.short)}</span><span class="club-status ${c.status}">${label[c.status]}</span></div><h3>${esc(c.name)}</h3><div class="club-meta">${esc(c.country)} · ${esc(c.league)}</div><p>${esc(c.summary)}</p><div class="tag-row">${c.tags.slice(0,3).map(t=>`<span>${esc(t)}</span>`).join('')}</div><div class="completion"><div class="completion-head"><span>Erfassungsstand</span><b>${c.completion}%</b></div><div class="completion-bar"><i style="width:${c.completion}%"></i></div></div></a>`).join('')||'<div class="empty-note">Keine Klubs entsprechen diesem Filter.</div>';}[search,scope,status].forEach(el=>el.addEventListener(el===search?'input':'change',render));render();document.querySelector('#clubCount').textContent=`${clubs.length} Klubs`;document.querySelector('#irishCount').textContent=clubs.filter(c=>c.scope.includes('Irland')).length;document.querySelector('#clCount').textContent=clubs.filter(c=>c.scope.includes('Champions League')).length;document.querySelector('#partialCount').textContent=clubs.filter(c=>c.status==='partial').length;document.querySelector('#completeCount').textContent=clubs.filter(c=>c.status==='complete').length;